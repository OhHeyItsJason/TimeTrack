# Frontend

React + Vite app for TimeTrack.

## Run

```bash
npm install
npm run dev
```

## API base URL

Defaults to `http://localhost:8787` in local mode.

To override:

```bash
VITE_API_BASE_URL=https://your-backend-url
```
